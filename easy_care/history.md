# EasyCare 수정 이력

## 1. 모델 계층 업데이트 (`health_stat.dart`)
- `mealType`, `stepType` 대신 `type`으로 필드 통합
- Firestore `toMap()`, `fromFirestore()`에 `type` 파라미터 적용 반영

## 2. 대시보드 스크린 업데이트 (`dashboard_screen.dart`)
- 하단 혈압 수치 표시 글자 크기 상향 (혈당 대비 60% 비율 고려: 20px -> 38px)
- 회원 탈퇴 등 기타 기능 대비 메뉴 구조 확장성 검토

## 3. 입력 스크린 업데이트 (`input_screen.dart`)
- **지능형 OCR 입력 로직 개선**:
  - 사진 촬영 시 기존 필드 초기화 및 반복 촬영 로직 강화
  - 숫자 1개: 혈당값 자동 입력
  - 숫자 2개 이상: 조건에 따른 혈당 우선 추출
  - 숫자 3개 이상: 혈압 키워드가 있을 시 수축기(큰 값)와 이완기(작은 값) 자동 정렬 및 분리 추출
- 촬영 버튼 텍스트 변경: '사진으로 자동 입력' -> '사진 촬영 / 재촬영'

## 4. 통계 스크린 업데이트 (`stats_screen.dart`)
- 앱 상단에 리포트 다운로드 아이콘 버튼 추가
- **상세 로그 UI 개선**: 
  - `_showDayDetail` 함수에서 `상태(공복 등) | 메모: 내용` 형식으로 출력 방식 수정
  - 정상/낮음/높음 등 수치 상태에 따른 조건부 폰트 색상(혈당, 혈압 진단 정보 연동) 적용

## 5. 로그인 스크린 업데이트 (`login_screen.dart`)
- 카카오 버튼 텍스트를 '카카오로 로그인 / 회원가입'으로 명확하게 수정하여 인증 흐름 안내

## 6. 메뉴 카드 위젯 업데이트 (`menu_card.dart`)
- 터치 영역 내 중요 텍스트 크기를 어르신 친화적으로 19px에서 22px로 확대 (굵게)

## 7. 메인 파일 검토 (`main.dart`)
- 전반적인 테마 및 초기화 로직 확인 (특별한 수정 소요 없음)

## 8. 로드맵 우선순위 변경
- 사용자 요청에 따라 Step 5 잔여 작업(Excel/PDF 리포트 추출 등) 및 Step 6 보다, **Step 7 (소셜 연동 및 커뮤니티, 회원가입/카카오 로그인, 마이페이지, 회원탈퇴 기능)** 을 우선적으로 구현하도록 로드맵 일정을 조정함.

## 9. Step 7 1단계 및 2단계 구현 (마이페이지 및 카카오 로그인 연동)
- `shared_preferences` 패키지 추가하여 로컬 세션 유지
- **로그인 (`login_screen.dart`)**: 카카오 SDK(`UserApi.instance.loginWithKakaoTalk`)를 활용한 실제 인증 로직 적용 및 로그인 상태 유지 구현
- **마이페이지 (`my_page_screen.dart`) 신규 생성**:
  - 카카오 프로필 정보(닉네임, 이메일) 표시
  - 전역 다크모드 스위치 지원
  - 카카오 로그아웃(`UserApi.instance.logout`) 및 세션 초기화 구현
  - 회원 탈퇴 기능(`UserApi.instance.unlink`) 구현 및 Double Check 경고 팝업 적용
- **대시보드 (`dashboard_screen.dart`) 업데이트**:
  - 카카오 프로필에서 가져온 사용자 이름을 상단 인삿말에 표시하도록 `StatefulWidget`으로 변경
  - 기존 '로그아웃' 메뉴 카드를 '마이페이지'로 변경하고 `MyPageScreen`으로 라우팅 처리

## 10. Step 7 3단계 구현 (가족 연결 UI 및 스마트 푸시 알람)
- **푸시 알람 (`notification_service.dart`) 생성**:
  - `flutter_local_notifications` 및 `timezone` 패키지를 활용한 로컬 알림 서비스 구현
  - `input_screen.dart`에서 '식전' 측정 기록 저장 시 식후 측정을 유도하는 알람 스케줄링(시연을 위해 10초 뒤로 세팅) 자동 등록 구현
- **가족 연결 (`family_screen.dart`) 신규 생성**:
  - 내 연결 코드 표시 및 복사 기능 UI
  - 연결된 가족 리스트 및 가족 코드 입력 초대 UI 구축
  - `dashboard_screen.dart`의 '가족 연결' 메뉴에서 해당 화면으로 라우팅 연결

## 11. Step 7 가족 연결 UI 고도화 및 디자인 원칙(RULES.md) 전면 적용
- **가족 연결 기능 (`family_screen.dart`) 완벽 구현**:
  - `SharedPreferences`를 활용하여 6자리 영문/숫자 혼합 고유 가족 초대 코드 임의 생성 및 유지 로직 구현.
  - `flutter/services.dart`의 `ClipboardData`를 활용하여 내 연결 코드 클립보드 복사 기능 및 스낵바 알림 추가.
  - 가족 초대 코드 입력 다이얼로그 추가 및 연결된 가족 리스트 상태(동적 추가/삭제) 관리.
  - 삭제 시 실수 방지를 위한 'Double Check' 경고 다이얼로그 추가.
  - `RULES.md` 공통 디자인 시스템 전면 반영:
    - **Primary Color:** 화면 전체 테마(배경, 아이콘, 타이틀 등)에 메인 색상(`Color(0xFF0052CC)`) 통일 적용.
    - **터치 영역 및 가독성:** 모든 터치 가능한 요소의 최소 영역을 `60px`(`minimumSize`, `BoxConstraints` 활용)로 확보하고, 본문 폰트 최소 사이즈를 `18px`(제목 `22px` 이상)로 상향.
    - **인터랙션 및 곡률:** 터치 피드백을 위한 `AnimatedScale(scale: 0.95)` 반영. 대시보드 카드 `32px`, 메뉴 카드 및 리스트 `28px`, 기본 버튼 `16px` 모서리 곡률 통일.
    - **위험 상태 색상:** 가족 연결 해제 버튼 및 팝업 확인 버튼에 Danger 색상(`Colors.red.shade800`) 적용.

## 12. 안드로이드 빌드 환경 수정 (`android/app/build.gradle.kts`)
- `flutter_local_notifications` 패키지 요구사항 충족 및 의존성 충돌 해결을 위해 `coreLibraryDesugaring`에 사용되는 `desugar_jdk_libs` 버전을 `2.1.4`에서 `2.1.5`로 상향 적용하여 안드로이드 앱 빌드 오류 해결.

## 13. 가족 연결 기능 고도화 (데이터 공유 및 메모 기능)
- **가족 연결 (`family_screen.dart`)**:
  - 가족 코드 등록 시식별용 '메모(별칭)' 입력 다이얼로그 추가.
  - 리스트에서 연필(Edit) 아이콘을 눌러 메모 내용을 언제든 수정 가능하도록 구현.
  - 가족 리스트 상태를 `SharedPreferences`를 사용해 로컬 기기에 JSON 형태로 영구 보존하도록 연동.
- **데이터 모델 및 입력 (`health_stat.dart`, `input_screen.dart`)**:
  - `HealthRecord` 데이터 모델에 `creatorCode` 속성을 추가.
  - Firestore에 기록을 저장할 때, 내 가족 코드(`myFamilyCode`)를 함께 담아 업로드하도록 로직 수정.
- **통계 및 대시보드 (`stats_screen.dart`, `dashboard_screen.dart`)**:
  - `stats_screen.dart`에서 화면 진입 시 내 코드와 연결된 가족들의 코드를 배열로 묶어 Firestore에 `whereIn` 조건으로 질의하도록 변경하여, 가족들의 기록을 캘린더와 차트에 병합 노출. (이후 로컬 필터링으로 수정하여 색인 문제 해결)
  - `dashboard_screen.dart`에서는 메인 대시보드 성격에 맞게 내 코드(`myFamilyCode`) 데이터만 필터링하여 노출하도록 수정.

## 14. 데이터 내보내기 (Excel 리포트 추출) 기능 구현
- `excel`, `path_provider`, `share_plus` 패키지 추가.
- `stats_screen.dart`에 기간 설정(`showDateRangePicker`)을 통한 리포트 추출 UI 추가.
- 선택된 기간의 데이터를 '일시', '구분', '혈당', '혈압(수축기/이완기)', '메모' 컬럼을 갖춘 엑셀(`.xlsx`) 파일로 생성하고, OS 기본 공유 기능을 통해 내보낼 수 있도록 구현.

## 15. iOS 플랫폼 지원 설정 (v1.9)
- **`AppDelegate.swift` 업데이트**: `flutter_local_notifications` 패키지의 푸시 알람 수신을 위한 `UNUserNotificationCenter` 델리게이트 초기화 설정 추가.
- **`Podfile` 설정 업데이트**:
  - `GoogleMLKit/TextRecognitionKorean` Pod 의존성을 명시하여 iOS 기기에서도 한국어 OCR이 정상 동작하도록 지원.
  - 카카오 로그인 시 필요한 `KAKAO_NATIVE_APP_KEY`를 소스코드에 하드코딩하지 않고 `.env` 파일에서 읽어와 Xcode 빌드 설정(`xcconfig`)에 동적 주입하도록 Ruby 스크립트 작성.
- **가이드 문서 갱신 (`AGENTS.md`, `RULES.md`)**: iOS 플랫폼 빌드 및 환경변수 주입 관련 아키텍처 규칙 추가 반영.

## 16. 유지보수 및 최신 API 마이그레이션 (v2.0)
- **전역 진단 및 에러 해결**:
  - `flutter analyze`를 통한 전역 코드 품질 검사 및 잠재적 버그 수정.
  - `test/widget_test.dart` 내 잘못된 앱 클래스명(`MyApp` -> `EasyCareApp`) 수정으로 테스트 빌드 정상화.
- **최신 Flutter API 적용**:
  - Flutter 3.27 이상에서 권장되는 `Color.withValues(alpha: ...)` API를 적용하여 기존 deprecated된 `withOpacity` 경고 전면 제거.
  - `share_plus` (v11+) 최신 API인 `SharePlus.instance.share` 및 `ShareParams` 구조로 마이그레이션 완료.
- **코드 안정성 강화 및 데이터 정책 준수**:
  - `use_build_context_synchronously` 린트 규칙에 따라 비동기 작업 후 `context.mounted` 체크 로직 강화 (`family_screen.dart`).
  - 마이페이지 내 회원 탈퇴(`_handleWithdrawal`) 시, 사용자의 `creatorCode`와 매칭되는 모든 Firestore 건강 기록을 `WriteBatch`로 일괄 삭제하는 로직 구현 완료 (개인정보 보호 정책 준수).
- **빌드 환경 최적화**:
  - 불필요한 빌드 아티팩트 및 라이브러리 예제 코드에 의한 진단 노이즈 제거를 위해 `build/` 디렉토리 정리 수행.

## 17. 당화혈색소(HbA1c) 추가 및 의료 약어 반영 (v2.1)
- **데이터 모델 확장**: `HealthRecord` 모델에 `hba1c` (double) 필드 추가.
- **입력 기능 고도화**:
  - `InputScreen`에 당화혈색소(HbA1c) 입력 필드 추가 (소수점 입력 지원).
  - OCR 로직에서 소수점 포함 숫자를 인식하여 당화혈색소 후보로 자동 매핑하는 기능 추가.
- **의료 표준 약어 적용**:
  - 앱 전반(대시보드, 입력, 통계)의 레이블에 실제 병원에서 사용하는 영어 약어 추가: **혈당 (BST)**, **혈압 (BP)**, **당화혈색소 (HbA1c)**.
- **UI/UX 개선**:
  - 대시보드 및 통계 상세 로그에 당화혈색소 기록 노출 추가.
  - 입력 화면에서 당화혈색소의 특성을 고려하여 전용 아이콘(`analytics`) 및 테마 색상(`purple`) 적용.

## 18. 분석 환경 최적화 (v2.2)
- **분석 노이즈 제거**: `analysis_options.yaml` 파일에 `build/**` 경로를 `exclude` 대상으로 추가하여, 외부 라이브러리(cloud_firestore 등)의 예제 및 테스트 코드에서 발생하는 불필요한 진단 에러가 에디터에 노출되지 않도록 조치함.
- **가이드 문서 업데이트**: `RULES.md`에 분석 제외 관련 프로젝트 규칙 추가.

## 19. 프로젝트 리팩토링 및 불필요 파일 제거 (v2.5)
- **중복 코드 및 파일 정리**: 
  - `InputScreen` 내부에 OCR 로직이 통합됨에 따라, 더 이상 사용되지 않는 `lib/services/ocr_service.dart` 파일을 삭제하여 프로젝트 구조 슬림화.
  - M1 맥북 개발 환경 최적화를 위한 `Podfile` v2.4 표준화 완료 및 시뮬레이터 대응 로직 정비.
- **가이드 문서 최신화**: `AGENTS.md`, `RULES.md`에 최신 파일 구조 및 M1 개발 표준 반영 완료.

## 20. 리포트 고도화 및 세션 관리 최적화 (v2.7)
- **웹 호환성 강화**: Flutter Web 지원을 위해 `web/index.html` 내 카카오 SDK 스크립트 추가 및 무결성(Integrity) 값 최신화.
- **건강 분석 리포트 확장**:
  - 통계 요약 탭에서 기간별 **평균 당화혈색소(HbA1c)** 정보를 자동으로 노출하도록 개선.
  - 엑셀/CSV 리포트 추출 시 당화혈색소 컬럼을 추가하여 의료 데이터 전문성 강화.
- **인증 세션 관리 최적화**:
  - 브라우저 쿠키로 인한 자동 로그인 문제를 해결하기 위해, 로그인 시 항상 계정 정보를 입력하도록 `Prompt.login` 옵션 강제 적용 (`login_screen.dart`).
- **가이드 문서 업데이트**: `AGENTS.md`, `RULES.md`를 v2.7로 상향하고 세션 정책 추가.

## 21. 웹 개발 환경 표준화 및 포트 고정 (v2.8)
- **VS Code 실행 설정 최적화**: 
  - 웹 실행 시마다 포트가 바뀌어 카카오 로그인 도메인 인증이 풀리는 문제를 해결하기 위해 `.vscode/launch.json` 파일을 생성.
  - 웹 테스트 포트를 항상 **`8080`**으로 고정하여 카카오 디벨로퍼 설정과 동기화함.
- **프로젝트 규칙 반영**: `RULES.md`에 웹 개발 시 포트 고정 정책을 명문화하여 협업 및 유지보수 효율성 증대.

## 22. 위험 알림 시스템 및 UI 최적화 (v3.0)
- **통합 위험 감지 (`isDanger`) 구현**: `HealthRecord` 모델에 혈당, 혈압, 당화혈색소 중 하나라도 위험 수준일 경우를 감지하는 로직 추가.
- **대시보드 경고 배너**: 위험 상태 감지 시 메인 화면 상단에 빨간색 경고 배너를 노출하고 카드 색상을 강조하여 시인성 극대화.
- **텍스트 잘림 해결**: 대시보드 내 혈압 표시부의 텍스트 레이아웃을 개선하여 고혈압/저혈압 등 상태 정보가 잘리지 않도록 수정.

## 23. 웹 환경 안정화 및 가족 데이터 공유 (v3.1)
- **크롬(웹) 환경 최적화**: 
  - `kIsWeb` 가드를 적용하여 웹에서 미지원되는 모바일 전용 기능(OCR 인식 엔진, 로컬 푸시 알림)의 호출을 차단하고 실행 정지 현상을 해결.
- **가족 기록 필터링 기능**:
  - `StatsScreen` 상단에 '전체', '나', '가족별' 필터 칩을 추가하여 특정 인물의 데이터만 골라볼 수 있는 기능 구현.
  - 상세 로그 리스트에 작성자 별칭(엄마, 딸 등)을 명시하여 데이터 소유주를 명확히 구분.
- **리포트 추출 업데이트**: 웹 환경에서는 모바일 전용 파일 시스템 접근 문제로 인해 추출 기능을 안내 메시지로 대체.

## 24. 가족별 맞춤 리포트 및 내비게이션 강화 (v3.2)
- **가족 화면 - 통계 연동**:
  - `FamilyScreen`의 가족 리스트 항목을 클릭 가능하게 개선하여, 클릭 시 해당 가족의 통계 화면으로 즉시 이동 및 자동 필터링 적용.
  - '내 기록 바로보기' 카드를 추가하여 본인의 데이터에 더 빠르게 접근할 수 있도록 UI 개선.
- **필터링 기반 리포트 다운로드**:
  - `StatsScreen`의 리포트 추출(`CSV`) 기능이 현재 선택된 필터(작성자)를 준수하도록 로직 수정.
  - 이제 특정 가족만 선택한 상태에서 다운로드 시 해당 인물의 데이터만 포함된 맞춤형 리포트 생성 가능.
- **추출 컬럼 표준화**: 리포트 파일의 가독성을 위해 헤더와 데이터 컬럼을 표준화함. (일시, 구분, BST, BP, HbA1c, 메모, 작성자)
- **가이드 문서 업데이트**: `AGENTS.md`, `RULES.md`를 v3.2로 상향하고 가족 데이터 열람 정책 구체화.

## 25. 서버 기반 고유 연결 코드 고정 (v3.3)
- **Firestore 사용자 관리 통합**: 
  - `users` 컬렉션을 신규 구축하여 카카오 ID를 키로 사용자 프로필과 가족 코드를 서버에 영구 보존.
  - 이제 브라우저 캐시 삭제나 `flutter clean` 후에도 로그인만 하면 이전과 동일한 내 코드를 즉시 복구 가능.
- **로그인 프로세스 고도화**:
  - `LoginScreen`에서 카카오 로그인 성공 시 Firestore를 조회하여 기존 코드를 불러오거나, 신규 유저일 경우에만 코드를 생성하여 저장하도록 로직 변경.
- **데이터 일관성 확보**: `FamilyScreen` 진입 시 서버 데이터와 로컬 `SharedPreferences`를 동기화하여 다중 기기 환경에서도 동일한 코드를 유지하도록 개선.
- **가이드 문서 업데이트**: `AGENTS.md`, `RULES.md`를 v3.3으로 상향하고 서버 데이터 관리 정책 반영.

## 26. 가족 화면 UI 간소화 및 통합 내비게이션 (v3.4)
- **UI 구조 최적화**: 
  - `FamilyScreen`에서 중복되던 '내 기록 바로보기' 카드를 제거하고, 가족 목록 최상단에 '나 (본인)' 항목을 고정 배치.
  - 모든 인물(나 + 가족)을 동일한 리스트 형식으로 통합하여 일관된 UX 제공.
- **내비게이션 통일**: 
  - 리스트 내 어떤 항목을 클릭하더라도 해당 인물의 통계 화면(`StatsScreen`)으로 즉시 이동 및 자동 필터링 적용.
  - '나' 타일은 수정/삭제 버튼을 제외하고 배경색을 차별화하여 본인임을 명확히 인지하도록 디자인.
- **가이드 문서 업데이트**: `AGENTS.md`, `RULES.md`를 v3.4로 상향하고 통합 리스트 정책 반영.

## 27. 필터 중복 해결 및 코드 클린업 (v3.6)
- 통계 필터 최적화 (v3.5): StatsScreen에서 "나" 항목이 중복으로 노출되던 로직을 수정하여 고유한 작성자 이름만 표시되도록 개선.
- 불필요한 임포트 제거 (v3.6): 린트(Lint) 경고에 따라 notification_service.dart에서 중복되는 material.dart 임포트를 삭제하여 코드 경량화 수행.
- 가이드 문서 업데이트: AGENTS.md, RULES.md를 v3.6으로 상향.

## 28. 환경 변수 설정 업데이트 (v3.7)
- .env 파일 내 KAKAO_JS_APP_KEY 설정 업데이트 및 보안 관리 강화.
- StatsScreen 필터 로직 개선: creatorCode가 없는 레거시 데이터를 '나'의 기록으로 포함하도록 수정 및 캘린더 뷰 필터 동기화.

## 29. 건강 기록 수정 기능 구현 (v3.8)
- StatsScreen 상세 내역 바텀시트에 '수정' 버튼 추가.
- InputScreen을 '등록/수정' 공용 모드로 확장: 기존 데이터를 전달받아 입력 필드를 미리 채워주는(Pre-fill) 기능 구현.
- Firestore update 로직 적용: 기존 문서 ID를 기반으로 데이터를 덮어씌우는 업데이트 기능 완성.

## 30. UI 안정성 및 내비게이션 고도화 (v3.9)
- 대시보드 최근 기록 카드 클릭 시 캘린더 해당 날짜로 즉시 이동하는 유기적 동선 구현.
- 가로 픽셀 오버플로우 방지를 위한 레이아웃 안정성 강화 (Expanded, softWrap 적용).
- 데이터 필터링 개선: creatorCode가 없는 레거시 데이터를 '나'의 기록으로 자동 분류.

## 31. 통계 그래프 가독성 개선 (v4.0)
- StatsScreen 차트 내 최고/최저 수치 라벨에 해당 기록의 날짜(YY/MM/DD)를 병기하여 시간적 맥락 제공.
- 가이드 문서 및 코딩 규칙 v4.0 상향 업데이트.

## 32. 대화형 툴팁 상세화 및 가독성 고도화 (v4.1)
- 그래프 점 클릭 시 나타나는 툴팁 정보 대폭 강화: 년/월/일 시:분, 측정 타입(공복/식전/식후 등), 사용자 메모 포함.
- 툴팁 내 정보 가독성을 위해 시간과 메모 사이에 줄바꿈 적용 및 이탤릭체 스타일링.
- "심플한 그래프, 상세한 툴팁" 디자인 원칙을 AGENTS.md 및 RULES.md에 반영.