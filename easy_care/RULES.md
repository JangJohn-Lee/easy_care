# 📜 EasyCare 코딩 및 협업 규칙 (v1.6)

## 1. UI/UX 및 테마 원칙
- **폰트/가독성:** 본문 최소 `18px`, 중요 수치 `bold` 및 `22px+` 적용. 터치 영역 최소 `60px`.
- **전역 다크모드:** 모든 페이지는 시스템 설정 혹은 전역 상태에 따라 다크모드가 즉각 반영되어야 함.
- **다국어 지원:** 시스템 설정을 따르되, 기본 언어는 **한글**로 고정함.
- **일관된 진단 로직:** 모든 스크린은 동일한 의학적 기준을 공유하며, 화면 간 상태 불일치는 절대 금지됨.

## 2. 지능형 OCR 입력 로직 (v1.6 추가)
- **사진 촬영:** 카메라 촬영 기능을 통해 input창에 자동 입력되어야 함.
- **추출 로직:**
  - **숫자 1개:** 해당 숫자를 `sugar`(혈당)로 자동 판단.
  - **숫자 2개 이상:** `혈당` 키워드 검색 후 인접한 숫자를 혈당 수치로 판단.
  - **숫자 3개 이상:** `혈압` 키워드가 있는 곳의 숫자를 수축기(큰 값)와 이완기(작은 값)로 판단.
- **재촬영:** 재촬영 버튼을 상시 노출하며, 재촬영 시 기존 필드를 초기화하고 로직을 재수행함.

## 3. 화면별 특화 규칙
### Dashboard
- 최근 혈당을 메인으로 하되, 혈압 수치를 하단/측면에 병기 (혈당 대비 60~70% 크기).

### StatsScreen (그래프)
- **차트:** 혈당/혈압 그래프 분리(Toggle). 선은 가늘게 하되 데이터 점(Dot) 강조.
- **기준선:** 정상 범위에 가로 점선(Baseline)을 가시적으로 표시.
- **요약:** 그래프 아래에 해당 기간의 최고/최저(혈당/혈압) 수치를 미관을 해치지 않게 표시.
- **툴팁:** 점 클릭 시 `혈당-숫자` 또는 `수축기-숫자 / 이완기-숫자` 출력.
- **필터:** 일간/주간/월간/연간/지정기간 요약 시 선택된 타입(혈당/혈압)에 맞는 요약 텍스트 출력.

### StatsScreen (상세 리스트)
- **조건부 색상:** 수치 상태(정상/낮음/높음)에 따라 숫자 폰트 색상을 다르게 출력.
- **형식:** `상태(공복 등) | 메모: 내용` 구조로 출력 (구분자 `|` 사용).
- **텍스트:** 기존 '기록'이라는 표현 대신 사용자가 작성한 '메모' 내용 출력.

## 4. 데이터 저장 및 환경
- **저장 필드:** `type`, `sugar`, `systolic`, `diastolic`, `memo`, `timestamp` 필수 포함.
- **리포트:** 지정 기간 데이터를 **Excel 또는 PDF** 형태로 다운로드 기능 구현.
- **Auth:** 로그아웃 시 세션 초기화 및 초기 진입점 강제 이동.
- **인프라:** Firebase Cloud Firestore (asia-northeast3).